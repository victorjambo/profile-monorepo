{
  "name": "models",
  "version": "0.0.0",
  "private": true,
  "types": "src/index.ts",
  "main": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsup",
    "lint": "eslint src/",
    "dev": "tsup --watch",
    "check-types": "tsc --noEmit",
    "test": "echo 'Add test script here'"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "tsup": "^6.1.3",
    "typescript": "^5.1.6"
  }
}
